interface:
  display_name: "Nginx"
  short_description: "Repo-specific patterns and workflows for nginx"
  default_prompt: "Use the nginx repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true